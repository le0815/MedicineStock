@model IEnumerable<MedicineStock.Models.Permission>
@* @model MedicineStock.Models.Prescription *@
@{
    ViewData["Title"] = "Index";
    ViewBag.Menu = "Permission";
}

<!-- header -->
<div class="page-header">
    <div class="page-title">
        <h2>Danh sách quyền</h2>
    </div>
    <div class="page-btn">
        <a class="btn btn-added" asp-action="Create"><img src="/assets/img/icons/plus.svg" alt="img" class="me-1">Thêm mới</a>
    </div>
</div>

<!-- search bar -->
<div class="table-top">
    <div class="search-set">
        <div class="search-input">
            <a class="btn btn-searchset"><img src="/assets/img/icons/search-white.svg" alt="img"></a>
            <input name="searchPhrase" type="search" class="form-control form-control-sm" placeholder="Tìm kiếm..." id="searchBox">
        </div>
    </div>
</div>


<div class="card mb-0">
    <div class="card-body">
        <div class="table-responsive dataview">
            <table class="table datatable " id="searchTable">
                <thead>
                    <tr>
                        <th>STT</th>
                        <th>Mã quyền</th>
                        <th>Quyền</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        int count = 1;
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>@count</td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.PermissionId)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td>
                                    <a asp-action="Edit" asp-route-id="@item.PermissionId">Sửa</a> |
                                    <a asp-action="Details" asp-route-id="@item.PermissionId">Chi tiết</a> |
                                    <a asp-action="Delete" asp-route-id="@item.PermissionId">Xoá</a>
                                </td>
                            </tr>
                            count++;
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
