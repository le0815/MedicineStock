@model IEnumerable<MedicineStock.Models.Account>
@* @model MedicineStock.Models.Prescription *@
@{
    ViewData["Title"] = "Index";
    ViewBag.Menu = "Account";
}

<!-- header -->
<div class="page-header">
    <div class="page-title">
        <h2>Danh sách tài khoản</h2>
        <h6>Quản lí tài khoản của bạn</h6>
    </div>
    <div class="page-btn">
        <a class="btn btn-added" asp-action="Create"><img src="/assets/img/icons/plus.svg" alt="img" class="me-1">Thêm mới</a>
    </div>
</div>

<!-- search bar -->
<div class="table-top">
    <div class="search-set">
        <div class="search-input">
            <a class="btn btn-searchset"><img src="/assets/img/icons/search-white.svg" alt="img"></a>
            <input name="searchPhrase" type="search" class="form-control form-control-sm" placeholder="Tìm kiếm..." id="searchBox">
        </div>
    </div>
</div>


<div class="card mb-0">
    <div class="card-body">
        <div class="table-responsive dataview">
            <table class="table datatable " id="searchTable">
                <thead>
                    <tr>
                        <th>STT</th>
                        <th>Mã tài khoản</th>
                        <th>Tên đăng nhập</th>
                        <th>Mật khẩu</th>
                        <th>@Html.DisplayNameFor(model => model.Email)</th>
                        <th>Quyền</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        int count = 1;
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>@count</td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.AccountId)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.UserName)
                                </td>
                                <td>
                                    **********
                                </td>
                                <td>
                                    **********
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Permission.Name)
                                </td>
                                <td>
                                    <a asp-action="Edit" asp-route-id="@item.AccountId">Chỉnh sửa</a> |
                                    <a asp-action="Details" asp-route-id="@item.AccountId">Chi tiết</a> |
                                    <a asp-action="Delete" asp-route-id="@item.AccountId">Xoá</a>
                                </td>
                            </tr>
                            count++;
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
